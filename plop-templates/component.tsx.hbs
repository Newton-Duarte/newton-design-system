import { cva, type VariantProps } from 'class-variance-authority'
import { HTMLAttributes } from 'react'
import { cn } from '../../lib/utils'

const {{ lowerCase name }}Variants = cva(
  '',
  {
    variants: {
      variant: {
        default: '',
      },
    },
    defaultVariants: {
      variant: 'default',
    },
  }
)

export interface {{ pascalCase name }}Props
  extends HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof {{ lowerCase name }}Variants> {}

export function {{ pascalCase name }}({ className, variant, ...props }: {{ pascalCase name }}Props) {
  return (
    <div
      className={cn({{ lowerCase name }}Variants({ variant, className }))}
      {...props}
    />
  )
}
